# RAG App Frontend

React + TypeScript frontend for PDF ingestion and RAG question answering via Inngest.

## Setup

1. Install dependencies.
2. Copy `.env.example` to `.env` and adjust if needed.
3. Run the dev server.

## Scripts

- `npm run dev`
- `npm run build`
- `npm run preview`
